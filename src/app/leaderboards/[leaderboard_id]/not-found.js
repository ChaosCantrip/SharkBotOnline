import NotFoundTemplate from "@components/NotFoundTemplate";

export default function LeaderboardNotFound() {
    return (
        <NotFoundTemplate title="Leaderboard not found!"
                          body="It appears the Leaderboard you are looking for does not exist!"/>
    )
}
